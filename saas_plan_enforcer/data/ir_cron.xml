<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron Job: Limpiar contadores de emails viejos -->
        <record id="cron_cleanup_email_counters" model="ir.cron">
            <field name="name">SaaS: Cleanup Old Email Counters</field>
            <field name="model_id" ref="mail.model_mail_mail"/>
            <field name="state">code</field>
            <field name="code">model._cron_cleanup_old_email_counters()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=0, minute=5)"/>
        </record>
    </data>
</odoo>
