<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron Job: Sincronizar límites del plan nocturnamente -->
        <record id="cron_sync_plan_limits" model="ir.cron">
            <field name="name">SaaS: Sync Plan Limits (Nightly)</field>
            <field name="model_id" ref="model_saas_plan_manager"/>
            <field name="state">code</field>
            <field name="code">model._cron_sync_plan_limits()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=0, minute=5)"/>
        </record>
    </data>
</odoo>
